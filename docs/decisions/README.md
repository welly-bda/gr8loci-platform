# Decisions (ADRs)

One file per decision. Naming: `NNNN-short-slug.md` (e.g. `0001-use-turborepo.md`).

Each ADR captures: context, decision, alternatives considered, consequences. Keep them short — decisions, not essays.

Status values: `proposed`, `accepted`, `superseded by NNNN`, `deprecated`.
